/**
 * 足球队单循环赛组合问题:
 * 有 4 支足球队进行单循环赛(甲、乙、丙、丁)
 * 1. 列出所有比赛场次的上场方;
 * 2. 计算所有冠亚军出现的情况有多少种;
 */

const team = ["甲", "乙", "丙", "丁"];

const competition = function (team) {
  // to do
};

const champion = function (team) {
  // to do
};
